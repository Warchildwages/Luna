/**
 * Register Luna on Casper MCP for agent discovery.
 */
export async function registerLunaOnCasperMCP(): Promise<boolean> {
  // Registration happens at runtime via x402 service-info
  return false;
}
